<?php

/**
 * @file
 * Primary module hooks for lwblocks module.
 *
 */

/**
 * Implements hook_theme().
 */
function lwblocks_theme($existing, $type, $theme, $path) {
  return [
    'sidebar-contact-block' => [
        'variables' => ['result' => NULL],
    ]
  ];
}